
  "name": "my-frontend",
  "version": "1.0.0",
  "scripts": {
    "start": "react-scripts start",   // 👈 used only for local dev
    "build": "react-scripts build",   // 👈 used for production build
    "test": "react-scripts test"
  },
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  }
}
